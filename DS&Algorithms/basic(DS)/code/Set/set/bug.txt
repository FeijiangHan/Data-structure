set：
如果多个数据结构在一个文件夹，注意区分不同节点的命名，都命名为Node会发生冲突
多态的实现需要派生类公有继承基类


 分析两种set的复杂度：添加删除查找
 底层链表：O(n)
 底层红黑树： O(logn)

作用：
处理大数据中的去重操作：背单词个数，统计文本单词个数，统计id，统计ip。多次出现但只需要得到种类个数的。

红黑树本身是BST，添加时已经去重了，更适合做集合的底层。

缺点：
链表的缺点是时间复杂度高，效率低
红黑树的缺点是只能处理可比较的元素

